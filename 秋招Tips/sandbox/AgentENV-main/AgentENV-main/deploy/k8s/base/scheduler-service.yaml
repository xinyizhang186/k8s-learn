apiVersion: v1
kind: Service
metadata:
  name: agentenv-scheduler
  labels:
    app.kubernetes.io/name: agentenv-scheduler
spec:
  selector:
    app.kubernetes.io/name: agentenv-scheduler
  ports:
    - name: grpc
      port: 9090
      targetPort: grpc
