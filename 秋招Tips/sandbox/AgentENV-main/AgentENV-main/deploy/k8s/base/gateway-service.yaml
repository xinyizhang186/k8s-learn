apiVersion: v1
kind: Service
metadata:
  name: agentenv-gateway
  labels:
    app.kubernetes.io/name: agentenv-gateway
spec:
  selector:
    app.kubernetes.io/name: agentenv-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
