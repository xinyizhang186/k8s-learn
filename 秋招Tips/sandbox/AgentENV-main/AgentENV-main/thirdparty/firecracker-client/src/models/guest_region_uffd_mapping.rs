/*
 * Firecracker API
 *
 * RESTful public-facing API. The API is accessible through HTTP calls on specific URLs carrying JSON modeled data. The transport medium is a Unix Domain Socket.
 *
 * The version of the OpenAPI document: 1.15.1
 * Contact: firecracker-maintainers@amazon.com
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// GuestRegionUffdMapping : Describes a guest memory region mapping, providing the host virtual address, region size, offset within a contiguous snapshot layout, and page size. Used by external processes to read guest memory directly from the Firecracker process address space via process_vm_readv().
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct GuestRegionUffdMapping {
    /// Base host virtual address of the guest memory region.
    #[serde(rename = "base_host_virt_addr")]
    pub base_host_virt_addr: i64,
    /// Region size in bytes.
    #[serde(rename = "size")]
    pub size: i64,
    /// Cumulative byte offset of this region within a contiguous layout of all guest memory regions. This is the position where this region's data starts if all regions are concatenated in iteration order, matching the offset semantics used in the uffd restore path (snapshot file layout). Note this is NOT the Guest Physical Address — GPAs may contain gaps (e.g. the MMIO hole near 4 GiB) absent from the contiguous layout.
    #[serde(rename = "offset")]
    pub offset: i64,
    /// Page size for this region (typically 4096).
    #[serde(rename = "page_size")]
    pub page_size: i32,
    /// Deprecated. Same value as page_size. Will be removed in 2.0.
    #[serde(rename = "page_size_kib", skip_serializing_if = "Option::is_none")]
    pub page_size_kib: Option<i32>,
}

impl GuestRegionUffdMapping {
    /// Describes a guest memory region mapping, providing the host virtual address, region size, offset within a contiguous snapshot layout, and page size. Used by external processes to read guest memory directly from the Firecracker process address space via process_vm_readv().
    pub fn new(
        base_host_virt_addr: i64,
        size: i64,
        offset: i64,
        page_size: i32,
    ) -> GuestRegionUffdMapping {
        GuestRegionUffdMapping {
            base_host_virt_addr,
            size,
            offset,
            page_size,
            page_size_kib: None,
        }
    }
}
