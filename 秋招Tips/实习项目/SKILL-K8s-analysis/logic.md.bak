# AutoK8s 项目核心逻辑

> 项目路径：`./AutoK8s-analysis`
> 统一入口：`generate.py`（调用 `autok8s.cli.main`），根据传入参数自适应选择分析模式。

---

## 1. 版本分析流水线（`autok8s.version_analysis`）

从发布博客抓取特性与弃用项，对每条特性逐句分类后提取痛点、构建增强句，经正则+机翻回退生成价值分析，通过质量门校验后写出"版本分析"Sheet。

### 1.1 流程

```
博客 URL
  │
  ▼ fetcher.fetch_release_blog（含重试）
抓取博客 HTML → 提取特性列表与弃用项
  │
  ▼ web_search 补充上下文
联网搜索每个特性的背景资料
  │
  ▼ content_gen.generate_analysis（核心）
对每条特性逐句分类（feature/pain/benefit/stage/background/noise）
  → 提取痛点（现状）→ 构建增强句（本特性增强）→ 生成价值分析
  │
  ▼ quality.py 质量门校验
自检：痛点是否过于泛化？增强句是否含有效信息？
不可信段落会被清空
  │
  ▼ xlsx_writer.write_workbook
写出 "版本分析" Sheet
```

### 1.2 关键算法（`content_gen.py`）

- **句子分类**（`_classify_sentence`）：按优先级 noise > stage > feature > benefit > pain > background > other，含方案动词的句子优先归类为 feature
- **痛点提取**（`_extract_pain_point`）：优先取 pain 句 > background 句 > other 句；若无则从 feature 句反推（"此前无法…"/"此前缺乏…"）
- **增强句构建**（`_build_enhancement`）：格式为 `v{ver} 将 {name} 升级为 {stage}，{behavior}`，behavior 取自 feature 句 > benefit 句 > other 句
- **阶段冗余清洗**（`_clean_stage_redundancy`）：用正则移除句子中重复的阶段声明（"在 v1.36 中晋升为 GA"等）

### 1.3 质量门校验（`quality.py`）

质量门覆盖所有生成路径——博客回退路径必须通过 `verify_content` / `_verify_deprecation` 校验，未通过则读者字段留空，不可信内容不进入 xlsx。

- **痛点校验**：长度 ≥ 14 字，含问题词（无法/难以/缺乏/不一致/风险/限制/依赖/失败/不足），非模板前缀
- **增强句校验**：长度 ≥ 20 字，不含多余历史陈述，非泛化表述
- **价值分析校验**：长度 ≥ 14 字，含收益词（提升/降低/简化/消除/改善/保护/防止/避免/确保/减少/加速/统一/缓解/收敛/隔离/恢复/增强/替代/实现/保障/支持/允许），非功能描述/痛点陈述/背景历史
- **价值兜底**：当正则管线未提取到含收益词的价值句时，返回空值而非任意句子，由质量门统一拦截
- **中文占比**：三段拼接后中文占比 ≥ 20%

---

## 2. 特性变更流水线（`autok8s.feature_changes`）

解析 `kube_features.go` 对比版本区间内 Feature Gate 的阶段与默认值变化，按 pkg.go.dev → KEP → CHANGELOG 三级取证链路补齐排查方法与详细说明，生成"特性变更"Sheet 及逐条核查 JSON。

### 2.1 流程

```
kube_features.go + 版本范围（如 1.35 1.36）
  │
  ▼ parser.parse
解析 Go 文件，提取所有 Feature Gate 的名称、阶段、默认值、prelock
  │
  ▼ analyzer.analyze
对比两个版本，逐条推导变更类型：
  - Added（新增门控）
  - Changed（阶段/默认值变化，如 Alpha→Beta、Beta→GA）
  - Deprecated（弃用）
  │
  ▼ research / fetcher 抓取资料
统一取证链路：
  1. 先读 pkg.go.dev/k8s.io/kubernetes/pkg/features 中的特性键、描述和 KEP 链接
  2. 按键检索 Kubernetes Enhancements Proposals
  3. 仅当包文档没有该特性时，回退到 data/CHANGELOG-<版本>.md
  │
  ▼ narrative.generate
翻译为中文，生成排查方法、详细说明、兼容分析等列
  │
  ▼ xlsx_writer
写出 "特性变更" Sheet（14 列）；--audit 指定时另写出逐条核查 JSON
```

### 2.2 兼容性判定

- 默认值变化或行为变更 → 标记"不兼容（需排查）"
- 开关状态不变或默认关闭 → 标记"兼容"
- 当"兼容分析"列有内容时，后续列全部留空

### 2.3 资料查找容错（`content_store.lookup`）

`lookup` 函数调用 `collect_feature_evidence` 抓取在线资料时，预先初始化 `online` 为空字典。当抓取过程抛出异常（网络超时、解析失败等）时，不会因引用未定义变量而崩溃，而是安全降级返回已收集的部分字段。

---

## 3. 双 Sheet 合并（`autok8s.workbook`）

当同时传入 `--blog` 和 `--go-file` 时，将版本分析与特性变更两条流水线的结果合并到同一 xlsx 的两个工作表中，一次性输出完整的双 Sheet 报告。

---

## 4. 自适应入口（`autok8s.cli`）

根据命令行参数自动选择运行模式：

```
仅 --blog URL              → 版本分析（单 Sheet "版本分析"）
仅 --go-file + --range     → 特性变更（单 Sheet "特性变更"）
两者都给                   → 双 Sheet 合并工作簿
都不给                     → 报错退出
```

参数校验链路：`main()` 解析参数 → 校验 `--go-file` 与 `--range` 互为依赖 → 校验版本范围下界 ≤ 上界 → 博客模式下逐个抓取并校验（任一失败立即报错退出，不静默使用不完整数据）→ 分发到对应的 `_run_version_analysis` / `_run_feature_changes` / `_run_combined`。

输出路径：默认放入 `output/v{版本}/` 文件夹（版本取 `--range` 上界或博客版本号），使用 `--output` 可指定自定义路径。特性变更的核查 JSON 默认与 xlsx 同目录。

---

## 5. 数据流

```
kube_features.go（权威源）
    │
    ├──→ feature_changes 流水线 → 特性变更 xlsx
    │         ↑ 回退资料
    │    CHANGELOG-*.md / pkg-features.html
    │
    └──→ 每次生成写出 feature-change-audit-*.json（逐条核查）

博客 URL（在线）
    │
    └──→ version_analysis 流水线 → 版本分析 xlsx
              ↑ 正则+机翻回退
```

- **权威源优先**：`kube_features.go` 是阶段/默认值/移除的 100% 权威定义，直接来自 K8s 源码
- **回退机制**：pkg.go.dev 文档 → KEP 仓库 → CHANGELOG，三级回退确保资料完整性
- **核查留痕**：使用 `--audit` 可写出逐条核查 JSON，确保"详细说明"必有资料链接、每个不兼容条目都有排查方法

---

## 6. 初步 xlsx 文本优化流水线（按 `Prompt.md`）

`generate.py` 生成的初步 xlsx 中，机器列（特性名/阶段/默认值/兼容性）由代码解析权威源，100% 准确；但文本列（特性功能介绍/价值分析/排查方法/详细说明）走正则+机翻回退，存在 6 类失效模式：机翻错字截断、特性名被机翻破坏、"现状"非痛点、价值分析缺失/为功能描述、KEP 配错（按键名盲搜 GitHub issue）、KEP README 模板噪声泄漏。

优化流水线**只替换文本列**，机器列原样保留，分四阶段执行（详见 `Prompt.md`）：

### 6.1 流程

```
初步 xlsx（generate.py 生成）
  │
  ▼ 阶段 A：提取待优化行
读 xlsx → 抽出"兼容分析"为空的特性变更行 + 版本分析全部特性/弃用行
          → 每行带齐机器上下文（特性名/阶段/默认值/已有但不准的文本）
          → 导出 va_rows.json / fc_rows.json
  │
  ▼ 阶段 B：证据采集（并行，5-8 个 deep agent，每批 6-10 特性）
每特性：
  1. websearch_web_search_exa 语义搜索（描述理想页面，非关键词）
     → 命中正确 KEP 编号 + KEP README URL + 官方文档
  2. webfetch 抓 KEP README（format=markdown）
     → 仅提取 Motivation/Proposal/Design/Graduation/FeatureGate 段
     → 剥离 HTML 注释、PR 模板噪声
  3. （不充分时）webfetch 抓发布博客原文段或 CHANGELOG-<版本>.md
  │
  ▼ 阶段 C：文本生成（并行，证据驱动，不再联网）
按字段规范生成中文：
  - 版本分析行：{特性功能介绍, 特性功能价值分析}
  - 特性变更行：{分析结论, 排查方法, 参考资料, 详细说明, 补充说明}
  │
  ▼ 阶段 D：校验 + 写回 optimized.xlsx
六项硬约束校验 → 不过则留空绝不编造
              → 保留机器列，仅替换文本列
```

### 6.2 文本来源优先级

优化文本来自三层，按准确度从高到低：

| 来源 | 覆盖范围 | 准确度 | 用法 |
|------|---------|--------|------|
| **参考文件** `AI_agent/k8sv1.35-v1.36.xlsx` | v1.35-v1.36 范围内已人工复核的文本 | 100%（人工校验） | 直接合并（模糊匹配特性名） |
| **预生成文本**（基于官方博客描述生成） | 参考文件未覆盖的版本（如 v1.33/v1.34） | 高（官方中文博客为权威源） | 用 `autok8s.version_analysis.fetcher` 抓取博客官方中文描述，按字段规范生成 |
| **websearch 现场生成** | 参考文件和博客均未覆盖的新特性（如 v1.37 预发布） | 高（语义搜索命中正确 KEP） | `websearch_web_search_exa` + `webfetch` 采集证据后生成 |

### 6.3 字段规范（与参考文件风格对齐）

**版本分析 Sheet**（文本列 = 特性功能介绍 + 特性功能价值分析）：

| 字段 | 格式 | 字数 | 硬约束 |
|------|------|------|--------|
| 特性功能介绍 | `现状：<痛点>\n本特性增强：v<X.Y> 将 <特性名> 升级为 <GA/Beta/Alpha>，<具体行为>` | 现状 30-80；增强 60-150 | 现状必须是真痛点（含"此前无法/难以/缺乏/依赖…"）；增强必须含版本号+阶段+具体行为 |
| 特性功能价值分析 | 一句话收益概括 | 15-40 | 必须是收益（提升/降低/简化/消除…），不是功能描述 |

**特性变更 Sheet**（文本列 = 分析结论/排查方法/参考资料/详细说明/补充说明）：

| 字段 | 格式 | 硬约束 |
|------|------|--------|
| 排查方法 | 操作步骤：检查对象 + 动作 + 验证点 | 必须含检查动词（检查/确认/核对/查看/验证）；必须针对该特性具体对象，不得用通用模板"在各组件启动参数和配置文件中检索 X" |
| 参考资料 | `http://kep.k8s.io/<NNNN>` | 有详细说明必有参考资料 |
| 详细说明 | 该特性"做什么 + 影响什么对象" | 不得是排查方法的复述；不得含 KEP README 模板噪声 |

### 6.4 六项硬约束校验（阶段 D）

每条文本必须全过，不过则重采证据或留空：

1. **现状是痛点**：含"此前/过去/原本/无法/难以/缺乏/依赖…"等痛点词
2. **增强含版本+阶段**：含 `v1.\d+` + `GA/Beta/Alpha`
3. **价值是收益非功能**：不得出现"该特性使…/一个关键目标…/公关/租赁/吊舱"等机翻腔或功能描述
4. **排查方法非通用模板**：含检查动词且针对具体对象（非"在各组件启动参数和配置文件中检索 X"模板句）
5. **有详细说明必有参考资料**：KEP 为 null 时详细说明留空
6. **无机翻腔/模板噪声**：不得出现"公关"=PR、"租赁"=Lease、"吊舱"=Pod、"命名您的PR"、`<!--` HTML 注释、"一行PR"

### 6.5 关键设计

- **机器列不动**：`分类/特性名称/特性价值领域/变更类型/特性阶段变化/默认值变化/默认值锁定/是否兼容/兼容分析/建议开启？` 已由 autok8s 准确生成，原样保留
- **证据先于生成**：每条文本生成前必须有 websearch + webfetch 拿到的可引用 URL（KEP/blog/CHANGELOG/feature-gates 文档）
- **生成后即校验**：对照证据做六项硬约束检查，不过则重采证据或留空，绝不编造
- **并行控速**：5-8 个并行 deep agent，每 agent 处理 6-10 个特性；106 条特性变更约 6-9 分钟完成
- **富文本保留**：优化写回时对"特性功能介绍"列重新应用 `_make_rich_text`，保持"现状："/"本特性增强："标签加粗
