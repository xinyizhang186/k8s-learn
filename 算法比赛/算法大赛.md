算法大赛任务书

1. 问题描述

在真实大模型推理量化场景中，权重和激活的量化要求不同，因此在赛题任务书中要求分别对权重、激活（A和Attention Q/K/V）设计最优量化算法。要求满足真实推理场景权重离线量化、激活动态变化在线量化的实际情况。

问题1：给定矩阵输入A, B（格式为NVFP4），经4Bit转换算法f转换为HiF4，均反量化为FP32后做矩阵乘法。要求转换前后MatMul结果的MSE足够小。

问题2：给定Attention计算中的Q，K，V（格式为NVFP4），经4Bit转换算法f转换为HiF4，均反量化为FP32后做Attention。要求转换前后Attention结果的MSE足够小。（这里Attn_type = GQA）

4Bit转换算法挑战：

1) Scale层级断裂：NVFP4的FP32+E4M3两级scale无法直接映射到HiF4的E6M2+E1_8+E1_16三级层级，需完全重构统计域与缩放逻辑；

2) 值集空间错位：E2M1与E1M2的离散值集分布不同（如4->6 间隔 vs 0.25步长）、反量化-再量化引入二次截断误差；

3) Outlier敏感度跃升：block_size 16 -> 64 使单个异常值稀疏整组精度，需重新设计峰值隔离与层级补偿机制。


2. 优化目标

给定：
I. 题目1： Linear场景
        i. 校准与权重量化
            1) NVFP4格式的权重矩阵及其量化参数；
            2) 与该权重对应的若干组NVFP4校准激活数据.
        ii. 在线激活量化
            1) NVFP4格式的在线激活矩阵及其量化参数；

II. 题目2： Attention场景
        i. 校准
            1) 若干组NVFP4格式的校准Q、K、V数据；
        ii. 在线Attn量化
            1) NVFP4格式的在线Q、K、V矩阵及其量化参数；
目标：
    分别针对Linear和Attention场景开发NVFP4到HiF4的量化算法。算法可利用校准数据预先确定量化相关状态或参数，并在在线阶段对激活及Q、K、V进行动态量化。
    1. Linear场景
       对权重进行离线HiF4量化，并结合校准信息对在线激活进行动态HiF4量化，使量化后的Linear输出X_HiF4@W_HiF4'与原始NVFP4数据反量化后计算得到的X_NVFP4@W_NVFP4'之间的MSE尽可能低。
    2. Attention场景
        使用校准Q、K、V数据生成在线量化所需状态，并分别对在线Q、K、V进行动态HiF4量化，使量化后的Attention(Q_HiF4, K_HiF4', V_HiF4')与原始NVFP4数据反量化后计算得到的Attention(Q_NVFP4, K_NVFP4', V_NVFP4')之间的MSE尽可能低。


3. 数据说明

linear共50组数据，每组数据分为1份权重 +  5个校准集 + 5个测试集；
Atten共50组数据，每组分为q，k，v三组数据，每组数据包含5个校准集 + 5个测试集；
校准集均不参与评分，使用测试集进行评分，分数更高者排名靠前。单次提交总运行时间限制5分钟，不对单一用例运行做时间限制。

问题1 数据样例linear.pt：包含一组数据，数据为一个字典，包含Activation（矩阵A），Weight（矩阵B）的NVFP4量化参数，量化后的数据；

问题2 数据样例attn.pt：包含一组数据，数据为一个字典，包含Q, K, V的的NVFP4量化参数，量化后的数据。


4.  solution.py的接口函数

a) hif4_calibration_and_quantize_weight:  接收NVFP4格式的权重数据及校准激活函数，完成权重量化，并返回HiF4权重量化参数及供在线激活量化使用的状态参数；
b) hif4_dynamic_quantize_activation: 接收NVFP4格式的在线激活数据及校准阶段生成的状态参数，动态生成HiF4激活量化参数；
c) hif4_calibration_attention: 接收校准阶段的NVFP4格式Q、K、V数据，生成后续的Q、K、V在线量化所需的状态参数；
d) hif4_dynamic_quantize_q、hif4_dynamic_quantize_k、hif4_dynamic_quantize_v: 分别接收NVFP4格式的在线Q、K、V数据及对应状态参数，动态生成各自的HiF4量化参数；


5. 执行代码

比赛平台执行选手上传的算法代码，并记录以下关键指标：
a) 量化参数合法性校验: 判题脚本对选手输出的HiF4参数进行严格校验，包含scale_factor的E6M2格式约束、scale_lv2∈{1,2}、scale_lv3∈{1,2}、sign∈{-1, 0, 1}、mant∈{0,0.25,0.5,0.75,1.0,1.25,1.5,1.75}​；
b) 量化函数执行时间检验：本赛题将使用选手给出量化函数对所有数据进行量化验证，计算总时间，超过指定时间将会判定选手提交失败；
c) 大模型输出的score：本赛题采用业界通用的量化评测指标，即使用选手给出的量化函数对数据进行量化，要求转换前后MatMul结果的MSE足够小、要求转换前后Attention结果的MSE足够小。（这里Attn_type = GQA）；


6. 判题流程

1) 选手上传算法代码包。代码包必须包含solution.py文件，并按照接口定义实现以下6个函数：hif4_calibration_and_quantize_weight、 hif4_dynamic_quantize_activation、hif4_calibration_attention、hif4_dynamic_quantize_q、hif4_dynamic_quantize_k、hif4_dynamic_quantize_v，选手可在代码包中包含其他辅助代码文件。
2) 加载测试数据和标准HiF4量化函数，测试数据分为Linear和Attention两类，每类数据均包含标准数据和测试数据；
3) Linear校准与权重量化。对于每组Linear数据，调用选手的hif4_calibration_and_quantize_weight函数，输入NVFP4格式的权重及对应的校准激活数据，得到： 
    a) HiF4格式的权重量化参数
    b) 后续在线激活量化使用的activation_state
        i. activation_state状态参数要求。hif4_calibration_and_quantize_weight可根据权重及校准激活数据生成activation_state，用于后续在线激活量化。activation_state可保存量化算法所需的校准信息、缩放参数、固定权重信息等，但需满足以下格式要求
        ii. 由权重校准阶段生成，并传递给后续在线激活量化函数，可用于保存校准参数、缩放参数、固定权重等算法状态。状态仅允许由None、布尔值、整数、有限浮点数、字符串、CPU Tensor、list、tuple和以字符串为key的dict组成。 Tensor支持bool/ int8/ int16/ int32/ int64/ float16/ bfloat16/ float32,不得包含NaN、Inf、复数或梯度信息；状态最大嵌套深度为8，总节点数不超过4096.
4) Attention校准。对于每组Attention数据，调用选手的hif4_calibration_attention函数，输入若干组NVFP4格式的校准Q、K、V数据以及Head相关参数，得到后续在线量化使用的q_state、k_state、v_state。
5) activation_state会作为传参传入选手提供的hif4_dynamic_quantize_activation，q_state、k_state和v_state会传入选手提供的hif4_dynamic_quantize_q&k&v，选手可自定义传参内容以及传参处理方法，如无需传参，可将相关冗余参数设为None；
6) Linear在线测试。对于每个测试激活数据，将校准阶段生成的activation_state与当前NVFP4激活数据一起传入hif4_dynamic_quantize_activation，得到对应的HiF4激活量化参数；
7) Attention在线测试。对于每组测试Q、K、V数据，分别调用hif4_dynamic_quantize_q、hif4_dynamic_quantize_k、hif4_dynamic_quantize_v，并传入对应校准状态，得到Q、K、V的HiF4量化参数；
8) 计算选手Linear输出。将选手生成的HiF4权重和激活参数反量化，并计算：X_HiF4@W_HiF4'；
9) 计算选手Attention输出。将选手生成的HiF4 Q、K、V参数反量化，并计算：Attention(Q_HiF4, K_HiF4', V_HiF4')；
10) 计算标准HiF4基线。使用标准HiF4量化函数对相同测试数据进行量化，并分别计算标准Linear输出和标准Attention输出；
11) 计算每个测试用例的MSE及得分。分别计算标准HiF4输出和选手HiF4输出相对于原始NVFP4参考输出的MSE： MSE_STD， MSE_PLAYER，并计算得分： Score = (MSE_STD - MSE_PLAYER)/MSE_STD;
12) 汇总所有参数用例得分。分别统计Linear和Attention各测试用例得分，并汇总得到最终综合评分。校准数据仅用于生成离线量化结果和在线量化所需状态，不直接参与最终得分计算；
13) 结果合法性与异常处理。选手返回的Weight、Activation、Q、K、V量化参数必须符合HiF4格式要求；校准状态必须符合接口允许的数据格式。若单个测试用例出现超时、运行异常、缺失结果或返回非法HiF4参数，则该次提交失败。


7. 计算得分

本赛题使用选手给出量化函数得出的MSE score和标准HiF4量化函数得出的MSE score进行比较，将score提升百分比的总分作为选手得分；执行时间长，复杂度高的算法，可能会被超时，导致不计分。
a) 最终得分是所有用例的MSE score提升百分比的总和；
b) 在任意case中，当量化给出的参数不符合参数定义范围，本次提交无效，使用附录中的self_check.py检查算法输出是否符合要求;
c) 对于测试集中任意测试用例，当选手提交的算法的结果劣于标准HiF4量化函数的MSE，得分为负，会根据劣化程度进行扣分，差距越远，负分越多。

8. 参考资料

1) HiFloat Format for Language Model Inference

2) HiFloat Format for Language Model Inference
